spring:
  application:
    name: ged-boot

  datasource:
    url: jdbc:postgresql://localhost:5432/ged_db
    username: postgres
    password: Alpha55@@

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8080